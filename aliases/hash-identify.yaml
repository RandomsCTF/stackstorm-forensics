---
name: "forensics_hash-identifier"
action_ref: "forensics.hash-identifier"
description: "Identify a hash."
formats:
    - "identify hash {{ hash }}"
ack:
    disabled: true
execution:
    output: "the most likely types for that hash are {{ result }}."
